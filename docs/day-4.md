# Day 4: SQLite database

- Added SQLite schema for customers, jobs, invoices, and tickets.
- Added foreign keys, status checks, timestamps, indexes, and amount validation.
- Added deterministic seed data: 5 customers, 10 jobs, 10 invoices, and 10 tickets.
- Added SQL query helpers for joins, grouping, aggregates, and subqueries.
- Verified with in-memory SQLite tests.

Result: all database tests passed and Ruff passed.
