# Day 1 foundations

Day 1 demonstrates a Python class, an enum, dataclasses, return annotations, parameter annotations, and a typed test. The project also uses a local `.venv`, Git, GitHub remote tracking, and pre-commit hooks for formatting and basic repository hygiene.
